[package]
name = "rspandas"
version = "0.1.0"
edition = "2024"

[lib]
name = "_rust"
crate-type = ["cdylib", "rlib"]

[dependencies]
pyo3 = { version = "0.29.0", features = ["extension-module"] }

[profile.release]
opt-level = 3
lto = true
